{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Applied skill proposal",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "kind",
    "proposal_id",
    "skill_name",
    "candidate_digest",
    "proposal_digest",
    "evaluation_digest",
    "approval_digest",
    "target_skill",
    "applied_at",
    "committed",
    "application_digest"
  ],
  "properties": {
    "schema_version": {"type": "integer", "const": 1},
    "kind": {"type": "string", "const": "skill-application"},
    "proposal_id": {"type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"},
    "skill_name": {"type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"},
    "candidate_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "proposal_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "evaluation_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "approval_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "target_skill": {"type": "string", "pattern": "^\\.agents/skills/[a-z0-9]+(?:-[a-z0-9]+)*/SKILL\\.md$"},
    "applied_at": {"type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"},
    "committed": {"type": "boolean", "const": false},
    "application_digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"}
  }
}
